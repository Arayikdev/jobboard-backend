[build]
builder = "railway.cpp"

[build.packages]
apt = [
    "libmongocxx-dev",
    "libbsoncxx-dev",
    "libsasl2-dev",
    "libssl-dev",
    "libcurl4-openssl-dev",
]

[deploy]
start = "/build/server"
